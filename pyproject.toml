[tool.ruff]
target-version = "py312"
line-length = 100
exclude = [
  ".venv",
  "artifacts",
  "build",
  "dist",
  "build_*",
  "teste_*",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"
